spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:///C:/JS22/Proyectos/Taller/config-data
  security:
    user:
      name: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
server:
  port: 9000
